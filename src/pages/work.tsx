import { MainLayout } from "@/components/layout";
import { LayoutProps, NextPageWithLayout } from "@/types";
import * as React from "react";

export interface IWorkProps {}

const Work = (props: LayoutProps) => {
  return <div>Work</div>;
};
export default Work;
